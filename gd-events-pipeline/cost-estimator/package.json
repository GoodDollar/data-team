{
  "name": "gd-cost-estimator",
  "version": "1.0.0",
  "description": "GoodDollar event pipeline cost estimator — scans all contracts via HyperSync and outputs a BQ cost forecast CSV",
  "type": "module",
  "scripts": {
    "estimate": "tsx estimate.ts",
    "estimate:full": "tsx estimate.ts --full"
  },
  "dependencies": {
    "@envio-dev/hypersync-client": "^0.6.3",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.7.0",
    "typescript": "^5.9.3"
  }
}
